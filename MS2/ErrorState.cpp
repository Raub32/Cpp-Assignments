#include <iostream>
#include "ErrorState.h"

using namespace std;

namespace AMA{
    explicit ErrorState:: ErrorState(const char* errorMessage = nullptr){
        message = errorMessage == nullptr ? 0 : new char = errorMessage
    };

}
